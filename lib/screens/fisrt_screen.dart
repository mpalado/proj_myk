import 'package:flutter/material.dart';

class ScreenOne extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Make Purchase')),
      body: Center(
        child: Text('Make Purchase'),
      ),
    );
  }
}
